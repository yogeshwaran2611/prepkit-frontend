node_modules/
dist/
# Stale build metadata in a clean clone makes `tsc -b` silently no-op while dist/ is absent,
# which breaks the mandatory `npm run evaluate`. Never commit these.
*.tsbuildinfo
.next/
.env
.env.local
.cache-live/
# Local JSON store used when MONGODB_URI is unset — per-developer data, never shared.
.data/
coverage/
*.log
.DS_Store
kits.json
